# Apollo Federation

Demo of Apollo GraphQL Gateway Federation (v1) with TypeScript + Mongoose

## How to run

- cd users && npm i && npm run codegen && npm run dev
- cd products && npm i && npm run codegen && npm run dev
- cd gateway && npm i && npm run dev

Don't forget to have a running instance of MongoDB.
